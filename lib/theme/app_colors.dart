import 'package:flutter/material.dart';

// ============================================================================
// App Colors - Dual Harmony Theme
// ============================================================================
class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF6BBFAB);       // Teal Green
  static const Color primaryLight = Color(0xFF9DD5C8);  // Light Teal
  static const Color primaryDark = Color(0xFF4A9A88);   // Dark Teal

  // Secondary Colors
  static const Color secondary = Color(0xFF355070);     // Deep Blue
  static const Color secondaryLight = Color(0xFF5A7A9A);
  static const Color secondaryDark = Color(0xFF1E3448);

  // Accent Colors
  static const Color accent = Color(0xFFEAAC6F);        // Amber Yellow
  static const Color accentLight = Color(0xFFF5CDA3);
  static const Color accentDark = Color(0xFFD4914A);

  // Background & Surface
  static const Color background = Color(0xFFFAFAF8);    // Ivory
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF2F2EF);

  // Text Colors
  static const Color textPrimary = Color(0xFF1E1E1E);
  static const Color textSecondary = Color(0xFF5A5A5A);
  static const Color textHint = Color(0xFF9E9E9E);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Semantic Colors
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFA726);

  // Call Status Colors
  static const Color connecting = Color(0xFFFFA726);    // Orange
  static const Color onCall = Color(0xFF4CAF50);        // Green
  static const Color ended = Color(0xFF9E9E9E);         // Grey
}
