rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Emulator/dev: open access. Tighten for production.
      allow read, write: if true;
    }
  }
}
